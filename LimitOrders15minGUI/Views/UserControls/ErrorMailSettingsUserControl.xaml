<UserControl x:Class="ViertelStdTool.Views.Controls.ErrorMailSettingsUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro" 
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="350">
    <Grid>
        <Label x:Name="ImfoLabel" Content="Receiver:" Margin="25,100,0,0" FontSize="15" FontWeight="Thin" Height="265" VerticalAlignment="Top" HorizontalAlignment="Left" Width="310"/>
        <Label x:Name="UserIdentifierCredentialsfile" Content="{Binding AppModel.MailInCaseOfError, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Margin="25,125,0,0" FontSize="15" FontWeight="SemiBold" Height="240" VerticalAlignment="Top" HorizontalAlignment="Left" Width="300"/>
        <Controls:ToggleSwitch x:Name="AutomaticUpdate" Header="Mail in case of an error" Margin="30,0,0,0" Background="gray" OffSwitchBrush="Red" OnSwitchBrush="Green">
            <Controls:ToggleSwitch.IsChecked>
                <Binding Path="AppModel.MailInCaseOfErrorEnabled" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged"/>
            </Controls:ToggleSwitch.IsChecked>
        </Controls:ToggleSwitch>
    </Grid>
</UserControl>
